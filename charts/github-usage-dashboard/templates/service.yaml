apiVersion: v1
kind: Service
metadata:
  name: {{ include "gud.fullname" . }}
  labels:
    {{- include "gud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "gud.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
